<div class="container">
  <div class="row justify-content-center">
    <div class="col">
      <div class="jumbotron jumbotron-fluid trip-view-jumbo">
        <div class="container">
          <h1 class="display-2">Expenditures for {{destination.name}}</h1>
          <p class="lead">Add and edit your dream Destination!</p>
          <button class="btn btn-outline-danger">This is a Button</button>
        </div>
      </div>


    </div>
  </div>

  <div class="row justify-content-center">
    <div class="col justify-content-center">
      <div class=" flex-row justify-center">
        <button class="button-73" role="button"><a href="/dashboard/trip/{{trip.id}}/destination/{{destination.id}}/new">➕ New Expenditure</a></button>
      </div>
          {{#each expenditure}}
            <div class="card">
              <div class="card-body">
                <h5 class="card-title">{{name}}</h5>
                <p class="card-text">Event Type: {{event_type}}</p>
                <p class="card-text">Cost: {{price}}</p>
                <a href="/dashboard/trip/{{../TripData.id}}/destination/{{id}}" class="btn btn-primary">Edit Destination</a>
                <button type="click" class="btn btn-primary delete-expenditure" id="{{id}}" data-trip-id="{{../trip.id}}" data-destination-id="{{../destination.id}}">Delete</button>
              </div>
            </div>
          {{/each}}
    </div>
  </div>


</div>

<script src="/js/new-expenditure.js"></script>